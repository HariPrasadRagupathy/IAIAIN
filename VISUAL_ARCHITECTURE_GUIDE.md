# Architecture Visual Guide - IAIAIN

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE (Compose)                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ LaunchingScreen                                             │   │
│  │  ├─ LaunchingHeaderSection (Logo, Title, Subtitle)        │   │
│  │  ├─ FeaturesSection (3 Feature Cards)                     │   │
│  │  ├─ CountdownSection (Days, Hours, Minutes, Seconds)     │   │
│  │  ├─ EarlyAccessSection (Form with 5 inputs)              │   │
│  │  ├─ FooterSection (Social Links)                          │   │
│  │  └─ Dialogs (Error, Success)                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
              ↑ State: observes                ↓ Intent: sends
┌─────────────────────────────────────────────────────────────────────┐
│              PRESENTATION LAYER (State Management)                   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ LaunchingViewModel                                          │   │
│  │  ├─ StateFlow<LaunchingScreenState>                       │   │
│  │  │  ├─ Countdown values                                   │   │
│  │  │  ├─ Form fields (name, email, institution, role)      │   │
│  │  │  ├─ UI state (loading, submitting, dialogs)           │   │
│  │  │  └─ Validation errors                                  │   │
│  │  │                                                         │   │
│  │  ├─ handleIntent(LaunchingIntent)                         │   │
│  │  │  ├─ UpdateFullName, UpdateEmail, UpdateInstitution   │   │
│  │  │  ├─ UpdateRole, UpdateReferralCode                    │   │
│  │  │  ├─ UpdateAgreeToTerms                                │   │
│  │  │  ├─ SubmitEarlyAccessRequest                          │   │
│  │  │  ├─ ClearError, CloseSuccessDialog                    │   │
│  │  │  └─ InitializeCountdown                               │   │
│  │  │                                                         │   │
│  │  ├─ Countdown Timer (Coroutine-based)                    │   │
│  │  └─ Form Validation (Real-time)                          │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
              ↓ Requests Business Logic             ↑ Results
┌─────────────────────────────────────────────────────────────────────┐
│                    DOMAIN LAYER (Business Logic)                     │
│  ┌──────────────────────────────┬──────────────────────────────┐   │
│  │   Use Cases                  │   Models                     │   │
│  │  ┌────────────────────────┐  │  ┌────────────────────────┐ │   │
│  │  │ SubmitEarlyAccessUseCase  │  │ EarlyAccessRequest     │ │   │
│  │  │ ├─ Validate input     │  │  │ ├─ fullName            │ │   │
│  │  │ ├─ Validate email     │  │  │ ├─ email               │ │   │
│  │  │ └─ Call repository    │  │  │ ├─ institution         │ │   │
│  │  └────────────────────────┘  │  │ ├─ role                │ │   │
│  │  ┌────────────────────────┐  │  │ ├─ referralCode        │ │   │
│  │  │ ValidateEmailUseCase   │  │  │ └─ agreeToTerms        │ │   │
│  │  │ ├─ Check format       │  │  └────────────────────────┘ │   │
│  │  │ └─ Call repository    │  │  ┌────────────────────────┐ │   │
│  │  └────────────────────────┘  │  │ EarlyAccessResponse    │ │   │
│  │                              │  │ ├─ success             │ │   │
│  │   Repository Interface       │  │ ├─ message             │ │   │
│  │  ┌────────────────────────┐  │  │ ├─ accessCode          │ │   │
│  │  │ EarlyAccessRepository  │  │  │ └─ timestamp           │ │   │
│  │  │ ├─ submitEarlyAccess   │  │  └────────────────────────┘ │   │
│  │  │ └─ validateEmail       │  │                              │   │
│  │  └────────────────────────┘  │                              │   │
│  └──────────────────────────────┴──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
              ↓ Data Requests                    ↑ Data Responses
┌─────────────────────────────────────────────────────────────────────┐
│                      DATA LAYER (API/Database)                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ EarlyAccessRepository Implementation                         │  │
│  │  ├─ Wraps errors in Result<T>                              │  │
│  │  ├─ Handles exceptions                                     │  │
│  │  └─ Delegates to DataSource                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ EarlyAccessRemoteDataSource                                 │  │
│  │  ├─ submitEarlyAccessRequest()                             │  │
│  │  │  ├─ [MOCK] Returns mock response                       │  │
│  │  │  └─ [REAL] Makes HTTP POST request                     │  │
│  │  │                                                          │  │
│  │  └─ validateEmail()                                        │  │
│  │     ├─ [MOCK] Returns mock result                         │  │
│  │     └─ [REAL] Makes HTTP POST request                     │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ Network Layer (Ktor HttpClient)                             │  │
│  │  └─ POST /api/v1/early-access                             │  │
│  │  └─ POST /api/v1/validate-email                           │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                            ↓
                    ┌─────────────────┐
                    │   Backend API    │
                    │   (Future)       │
                    └─────────────────┘
```

---

## Data Flow Diagram

```
USER INTERACTION
     ↓
┌─────────────────────────────┐
│ User enters "John" in name  │
│ field and taps outside      │
└─────────────────────────────┘
     ↓
┌─────────────────────────────────────────────────────┐
│ UI calls:                                           │
│ onIntent(UpdateFullName("John"))                   │
└─────────────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────────┐
│ ViewModel.handleIntent()                                 │
│  └─ LaunchingIntent.UpdateFullName("John")              │
│      └─ updateFullName("John")                           │
│          ├─ Validate: name.isNotBlank() ✓              │
│          └─ Update state:                               │
│             state.copy(                                 │
│                fullName = "John",                       │
│                fullNameError = null                     │
│             )                                           │
└──────────────────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────────┐
│ StateFlow<LaunchingScreenState> emits new state          │
└──────────────────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────────┐
│ UI observes new state via collectAsState()               │
│  └─ Recomposes with new fullName value                  │
│  └─ TextField displays "John"                            │
│  └─ Error message cleared                               │
└──────────────────────────────────────────────────────────┘


FORM SUBMISSION FLOW
     ↓
┌──────────────────────────────┐
│ User taps Submit button       │
└──────────────────────────────┘
     ↓
┌──────────────────────────────────────────────┐
│ UI calls:                                    │
│ onIntent(SubmitEarlyAccessRequest)          │
└──────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────┐
│ ViewModel.handleIntent()                             │
│  └─ LaunchingIntent.SubmitEarlyAccessRequest        │
│      └─ submitEarlyAccessRequest()                   │
│          ├─ Update state: isSubmitting = true        │
│          ├─ Create EarlyAccessRequest from state    │
│          └─ Launch coroutine:                        │
│             val result = submitEarlyAccessUseCase() │
└──────────────────────────────────────────────────────┘
     ↓
┌─────────────────────────────────────────────────────────┐
│ UseCase.invoke(request)                                 │
│  ├─ Validate fullName.isNotBlank() ✓                  │
│  ├─ Validate email format ✓                           │
│  ├─ Validate agreeToTerms == true ✓                   │
│  └─ Call repository.submitEarlyAccessRequest()        │
└─────────────────────────────────────────────────────────┘
     ↓
┌───────────────────────────────────────────────┐
│ Repository.submitEarlyAccessRequest()         │
│  └─ Try {                                     │
│      val response = dataSource.submit()       │
│      Result.success(response)                 │
│     } Catch {                                 │
│      Result.failure(exception)                │
│     }                                         │
└───────────────────────────────────────────────┘
     ↓
┌───────────────────────────────────────────────┐
│ DataSource.submitEarlyAccessRequest()         │
│  ├─ [MOCK] Simulate network delay            │
│  ├─ [MOCK] Return mock response               │
│  └─ [REAL] Make HTTP POST /api/v1/early-access
└───────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────┐
│ Return Result<EarlyAccessResponse>           │
│  ├─ Success: Response with accessCode        │
│  └─ Failure: Exception with error message    │
└──────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────────┐
│ ViewModel receives result                                │
│  ├─ onSuccess {                                         │
│  │    state.copy(                                       │
│  │      isSubmitting = false,                           │
│  │      showSuccessDialog = true,                       │
│  │      successMessage = "Success!"                     │
│  │    )                                                 │
│  │    emit(LaunchingEffect.ShowSuccess(message))       │
│  ├─ onFailure {                                         │
│  │    state.copy(                                       │
│  │      isSubmitting = false,                           │
│  │      errorMessage = error.message                    │
│  │    )                                                 │
│  │    emit(LaunchingEffect.ShowError(error))           │
└──────────────────────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────────────────────┐
│ UI observes new state and shows Success Dialog           │
│  ├─ SuccessDialog(message = "Success!")                │
│  └─ User sees confirmation                             │
└──────────────────────────────────────────────────────────┘
```

---

## File Dependency Graph

```
┌─────────────────────────────────────────────────────────────┐
│                    APP.KT (Entry Point)                      │
│                           │                                  │
└─────────────────────────────────────────────────────────────┘
              │
              ├──→ Theme (IAIAINTheme)
              │
              ├──→ ServiceLocator (DI)
              │         │
              │         ├──→ LaunchingViewModel
              │         │         │
              │         │         ├──→ SubmitEarlyAccessUseCase
              │         │         ├──→ ValidateEmailUseCase
              │         │         │
              │         ├──→ EarlyAccessRepositoryImpl
              │         │         │
              │         └──→ EarlyAccessRemoteDataSourceImpl
              │
              └──→ LaunchingScreen (UI)
                        │
                        ├──→ LaunchingHeaderSection
                        │         │
                        │         └──→ Design System Colors
                        │         └──→ Design System Typography
                        │
                        ├──→ FeaturesSection
                        │         │
                        │         └──→ FeatureCard Component
                        │
                        ├──→ CountdownSection
                        │         │
                        │         └──→ CountdownItem Component
                        │
                        ├──→ EarlyAccessSection
                        │         │
                        │         ├──→ TextField Component
                        │         └──→ PrimaryButton Component
                        │
                        ├──→ FooterSection
                        │
                        └──→ Dialogs
                                │
                                ├──→ ErrorDialog
                                └──→ SuccessDialog
```

---

## State Updates Visualization

```
┌─────────────────────────────────────────┐
│   Initial LaunchingScreenState          │
├─────────────────────────────────────────┤
│ fullName: ""                             │
│ email: ""                                │
│ institution: ""                          │
│ role: ""                                 │
│ referralCode: ""                         │
│ agreeToTerms: false                      │
│ isSubmitting: false                      │
│ showSuccessDialog: false                 │
│ successMessage: ""                       │
│ errorMessage: null                       │
│ fullNameError: null                      │
│ emailError: null                         │
│ institutionError: null                   │
│ roleError: null                          │
└─────────────────────────────────────────┘
        ↓ UpdateFullName("John")
┌─────────────────────────────────────────┐
│   Updated LaunchingScreenState          │
├─────────────────────────────────────────┤
│ fullName: "John"  ← CHANGED             │
│ fullNameError: null  ← CLEARED          │
│ ... (other fields unchanged)             │
└─────────────────────────────────────────┘
        ↓ UpdateEmail("invalid")
┌─────────────────────────────────────────┐
│   Updated LaunchingScreenState          │
├─────────────────────────────────────────┤
│ email: "invalid"  ← CHANGED             │
│ emailError: "Invalid email format"      │
│              ← SET                       │
│ ... (other fields unchanged)             │
└─────────────────────────────────────────┘
        ↓ UpdateEmail("john@example.com")
┌─────────────────────────────────────────┐
│   Updated LaunchingScreenState          │
├─────────────────────────────────────────┤
│ email: "john@example.com"  ← CHANGED    │
│ emailError: null  ← CLEARED             │
│ ... (other fields unchanged)             │
└─────────────────────────────────────────┘
        ↓ SubmitEarlyAccessRequest
┌─────────────────────────────────────────┐
│   Updated LaunchingScreenState          │
├─────────────────────────────────────────┤
│ isSubmitting: true  ← LOADING START     │
│ ... (other fields unchanged)             │
└─────────────────────────────────────────┘
        ↓ Response Received
┌─────────────────────────────────────────┐
│   Updated LaunchingScreenState          │
├─────────────────────────────────────────┤
│ isSubmitting: false  ← LOADING END      │
│ showSuccessDialog: true  ← SHOW DIALOG  │
│ successMessage: "Thank you for..."      │
│            ← SET MESSAGE                │
│ ... (other fields unchanged)             │
└─────────────────────────────────────────┘
```

---

## Component Composition Tree

```
LaunchingScreen
├── Header
│   ├── Logo Box
│   │   └── Text ("✓")
│   ├── Title Row
│   │   ├── Text ("Something")
│   │   ├── Text ("Amazing", orange)
│   │   └── Text ("is Coming Soon")
│   └── Description
│       └── Text (subtitle)
├── Features Section
│   ├── FeatureCard 1
│   │   ├── Icon ("✓", green)
│   │   ├── Title ("Junior Hub")
│   │   └── Description
│   ├── FeatureCard 2
│   │   ├── Icon ("🏢")
│   │   ├── Title ("Campus Hub")
│   │   └── Description
│   └── FeatureCard 3
│       ├── Icon ("🌐")
│       ├── Title ("Global Network")
│       └── Description
├── Countdown Section
│   ├── Title ("Launch Countdown")
│   ├── CountdownItem (Days)
│   │   ├── Value ("30")
│   │   └── Label ("Days")
│   ├── CountdownItem (Hours)
│   │   ├── Value ("00")
│   │   └── Label ("Hours")
│   ├── CountdownItem (Minutes)
│   │   ├── Value ("00")
│   │   └── Label ("Minutes")
│   └── CountdownItem (Seconds)
│       ├── Value ("00")
│       └── Label ("Seconds")
├── Form Section
│   ├── Title ("Get Early Access")
│   ├── Description
│   ├── TextField (Name)
│   │   ├── Label
│   │   ├── Input
│   │   └── Error (if any)
│   ├── TextField (Email)
│   │   ├── Label
│   │   ├── Input
│   │   └── Error (if any)
│   ├── TextField (Institution)
│   │   ├── Label
│   │   ├── Input
│   │   └── Error (if any)
│   ├── TextField (Role)
│   │   ├── Label
│   │   ├── Input
│   │   └── Error (if any)
│   ├── TextField (Referral Code)
│   │   ├── Label
│   │   ├── Input
│   │   └── Error (if any)
│   ├── Checkbox + Label (Terms)
│   └── PrimaryButton (Submit)
│       └── Text or Loading Indicator
├── Footer Section
│   ├── Text ("Follow us")
│   └── Icon Buttons
│       ├── Facebook
│       ├── Twitter
│       ├── LinkedIn
│       └── Instagram
└── Dialogs
    ├── ErrorDialog (if errorMessage != null)
    │   ├── Title ("Error")
    │   ├── Message
    │   └── Button ("OK")
    └── SuccessDialog (if showSuccessDialog)
        ├── Title ("Success!")
        ├── Message
        └── Button ("Continue")
```

---

## Design System Hierarchy

```
Theme (IAIAINTheme)
├── Colors
│   ├── Primary (Blue Palette)
│   │   ├── PrimaryBlue #1B3A6B
│   │   ├── PrimaryDark #0F2340
│   │   └── PrimaryLight #2E5090
│   ├── Accent Colors
│   │   ├── AccentOrange #FFA500
│   │   ├── AccentGreen #2ED573
│   │   └── AccentPurple #9B59B6
│   ├── Backgrounds
│   │   ├── BackgroundDark #0A1929
│   │   ├── BackgroundMedium #1B3A6B
│   │   └── SurfaceLight #2E5090
│   ├── Text Colors
│   │   ├── TextWhite #FFFFFF
│   │   ├── TextGray #B0BEC5
│   │   └── TextDarkGray #90A4AE
│   └── Semantic
│       ├── Success (Green)
│       ├── Error (Red)
│       ├── Warning (Yellow)
│       └── Info (Blue)
├── Typography
│   ├── Headlines (32sp, 28sp, 24sp)
│   ├── Titles (22sp, 16sp, 14sp)
│   ├── Body (16sp, 14sp, 12sp)
│   └── Labels (14sp, 12sp, 11sp)
├── Spacing
│   ├── xs (4dp)
│   ├── sm (8dp)
│   ├── md (12dp)
│   ├── lg (16dp)
│   ├── xl (24dp)
│   ├── xxl (32dp)
│   └── xxxl (48dp)
├── Corner Radius
│   ├── sm (4dp)
│   ├── md (8dp)
│   ├── lg (12dp)
│   ├── xl (16dp)
│   └── full (999dp)
└── Components
    ├── Buttons
    ├── TextFields
    ├── Cards
    ├── Icons
    └── Dialogs
```

---

## Flow Summary

```
User Opens App
    ↓
App.kt loads → Creates ViewModel → Initializes Countdown
    ↓
LaunchingScreen renders → Shows header, features, countdown
    ↓
User fills form → ViewModel validates → Updates state → UI updates
    ↓
User submits → ViewModel calls UseCase → UseCase validates
    ↓
UseCase calls Repository → Repository calls DataSource
    ↓
DataSource returns result → Repository wraps in Result<T>
    ↓
ViewModel receives result → Updates state → Emits effect
    ↓
UI shows success or error dialog
    ↓
User dismisses dialog → Dialog closes → Form can be reused
```

---

This visual guide shows how all components, layers, and files work together in the IAIAIN architecture!

